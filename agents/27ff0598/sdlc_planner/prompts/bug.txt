/bug Slow authentication: When logging in to the app, the authentication takes a long time to complete. The site appears as loading for various seconds until finally it completes and the user can access the modules. We need the authentication to occur much faster so that, for example, the admin user can quickly access the contract generation functionalities or the finance invoicing functionalities. 