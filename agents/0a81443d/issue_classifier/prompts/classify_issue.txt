/classify_issue {"number":37,"title":"Tesorería - Output File Missing Columns and Incorrect Column Order","body":"# Bug: Tesorería - Output File Missing Columns and Incorrect Column Order\n\n## Bug Description\n\nThe Tesorería \"Plantillas para Cargar NetSuite\" feature for both Colombia and México is generating output files with:\n1. **Missing columns**: 5 columns are not present in the output\n2. **Incorrect column order**: The columns that exist are not in the correct order\n3. **Extra column**: There's a `memo` column that shouldn't be there\n\n## Current Output (WRONG)\n\nBoth Colombia and México output files currently have **10 columns** in this order:\n\n```\n1. payment_date\n2. customer_external_id\n3. payment_ref\n4. invoice_core_id\n5. concept_type\n6. payment_amount\n7. currency\n8. exchangerate\n9. araccount\n10. memo          ← Should not exist\n```\n\n## Expected Output (CORRECT)\n\nThe output files should have **14 columns** in this exact order (excluding the first \"Nombres de columnas\" header column from the template definition):\n\n```\n1. customer_external_id\n2. invoice_core_id\n3. concept_type\n4. payment_date\n5. payment_amount\n6. currency\n7. payment_ref\n8. account         ← MISSING\n9. araccount\n10. exchangerate\n11. comision_banco  ← MISSING\n12. Spread PA       ← MISSING\n13. Spread FK       ← MISSING\n14. Spread Supra    ← MISSING\n```\n\n## Reference File\n\nThe column order and definitions are specified in:\n- `Example Files for Reqs/FIN_ Definicion Template para Aplicación de pagos.xlsx`\n- Sheet: **Hoja 1**\n- Row 1 (index 0): Colombia column definitions\n- Row 2 (index 1): México column definitions\n\n## Specific Issues to Fix\n\n### 1. Missing Columns (5 columns)\n\n| Column | Description | Rule |\n|--------|-------------|------|\n| `account` | Bank account internal ID | Lookup from Catalogo CO/MX based on bank name or account number |\n| `comision_banco` | Bank commission | México only: from \"Referencia bancaria\" column, only on first line of each payment group |\n| `Spread PA` | PA Spread calculation | For manual payments: (Tasa de cambio FK/en línea - Tasa BanRep) × Total pagado USD. Only on one line per payment |\n| `Spread FK` | FK Spread calculation | Total pagado × Spread Finkargo rate. Only on one line per payment |\n| `Spread Supra` | Supra Spread calculation | Total pagado × Spread Finkargo rate. Only on one line per payment |\n\n### 2. Column Order Fix\n\nChange the DataFrame column order from current to expected:\n\n**Current order:**\n```python\n['payment_date', 'customer_external_id', 'payment_ref', 'invoice_core_id', 'concept_type', 'payment_amount', 'currency', 'exchangerate', 'araccount', 'memo']\n```\n\n**Expected order:**\n```python\n['customer_external_id', 'invoice_core_id', 'concept_type', 'payment_date', 'payment_amount', 'currency', 'payment_ref', 'account', 'araccount', 'exchangerate', 'comision_banco', 'Spread PA', 'Spread FK', 'Spread Supra']\n```\n\n### 3. Remove `memo` Column\n\nThe `memo` column should be removed from the output - it's not part of the template specification.\n\n## Implementation Notes\n\n### For `account` column:\n- Look up the internal ID from Catalogo CO or Catalogo MX based on the source file's bank information\n- Use \"Banco remitente\" or \"Cuenta Remitente\" columns from Historial de Pagos to match\n\n### For `comision_banco` column:\n- Colombia: Leave empty (NaN)\n- México: Extract from \"Referencia bancaria\" column, populate only on the FIRST line of each payment group (same payment_ref)\n\n### For Spread columns:\n- These should only be populated on ONE line per payment group\n- If payment method is \"Manual\": Calculate Spread PA\n- If payment method is \"Pago en línea\" (SUPRA/PA): Calculate Spread FK or Spread Supra based on Short Code\n- Values should be in local currency\n\n### Column Reordering:\nAfter building the DataFrame, explicitly set column order:\n```python\nEXPECTED_COLUMNS = [\n    'customer_external_id',\n    'invoice_core_id', \n    'concept_type',\n    'payment_date',\n    'payment_amount',\n    'currency',\n    'payment_ref',\n    'account',\n    'araccount',\n    'exchangerate',\n    'comision_banco',\n    'Spread PA',\n    'Spread FK',\n    'Spread Supra'\n]\ndf = df[EXPECTED_COLUMNS]\n```\n\n## Files to Modify\n\n- Backend service that generates the Excel output (likely in `backend/src/core/servicios/`)\n- Look for the payment template conversion service\n- The DataFrame construction and Excel export logic needs to be updated\n\n## Validation\n\nAfter fix, verify:\n1. Output has exactly 14 columns\n2. Columns are in the exact order specified above\n3. `memo` column is removed\n4. `account` column has values (lookup from catalog)\n5. Spread columns exist (can be empty/NaN where not applicable)\n6. `comision_banco` exists (can be empty/NaN for Colombia)\n\n## Steps to Reproduce\n\n1. Navigate to Tesorería → Plantillas para Cargar NetSuite → Colombia (or México)\n2. Upload a Historial de Pagos file\n3. Click \"Convertir\"\n4. Download the generated file\n5. Observe: Wrong column count (10 instead of 14), wrong order, missing columns\n"}